name: spine-migration-ci

on:
  push:
    branches:
      - "spine/upgrade-*"
  workflow_dispatch:

permissions:
  actions: read
  contents: write
  id-token: write

jobs:
  native-packages:
    uses: ./.github/workflows/spine-release.yml
    with:
      version: "0.4.1"
