# Migration review

Baseline: Spine `98ee6314e962daa90d83ad122031c0ae4340c2db`, upstream target `3d2ee51ca2d5db578f328aa75e20aa22c0197c9a`.

1. **P2 — saved SDK configuration selected too late.** `codex-rs/core/src/spine/config.rs:39` previously loaded a mutable external file during ConfigBuilder, before resume/fork history could supply its saved SDK configuration. Removing or corrupting the global source prevented startup/replay; export also reopened old source files. The repair represents pending and resolved configurations explicitly and resolves at session initialization, using the selected historical snapshot first. Exports use the resolved snapshot. Both removed/malformed global-source cases with export enabled passed in the complete workspace run.
2. **P2 — media-only anchor classification shifted.** `codex-rs/core/src/context/spine_context.rs:196` inserted an input-text anchor without updating the target release's positional content classifications. The repair uses AnnotatedContent when inserting the new item. A real image request checks both the anchor and the aligned classification array; it passed in the complete workspace run.
3. **P2 — genuine historical cold-replay coverage missing.** `codex-rs/core/src/spine/coordinator/replay.rs:8` previously had current-generated replay tests but no frozen valid v1 sampling archive from 0.3.x. A fixture was generated by the installed 0.3.3 binary, opened a real Spine branch and successfully cold-resumed under that binary. Its SHA-256 and binary provenance accompany the fixture. The target cold-resume integration passed in the complete workspace run.

The review found no additional confirmed defects in the inspected envelope, staged compact, Spawn lifecycle and TUI settlement paths. The existing compact persistence-failure, Spawn failure/retry/continuation and embedded app-server/TUI tests retain their behavioral assertions.

The large import consists primarily of the original customization tree, schema, snapshots and assets. It is divided into SDK, history, thread-store, core lifecycle, app-server, TUI and release commits. The smallest independent foundation was the SDK-only import. Review complete-history loading in `thread-store/src/local/model_context.rs` before the dependent sampling-boundary fork adaptation in `thread-store/src/local/paginated_fork.rs`; the post-review configuration lifecycle repair, media classification repair, historical fixture, and full-workspace fixture adaptations are separate follow-up stages. No remote history is rewritten.
